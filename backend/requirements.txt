# Backend del Generative Models Visual Lab — Mac Apple Silicon (MPS) / CPU. Nunca CUDA.
fastapi>=0.115
uvicorn[standard]>=0.30
torch>=2.5
# v3: features de InceptionV3 para la métrica KID del ciclo de calidad (app/experiments/)
torchvision>=0.20
# Datos e imágenes
numpy>=1.26
pillow>=10.0
huggingface_hub>=0.23
datasets>=2.19
# Análisis del espacio latente (PCA, k-means)
scikit-learn>=1.4
# Subida de imágenes (form-data)
python-multipart>=0.0.9
umap-learn
